{
  "name": "mental-health-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next export",
    "deploy": "npm run build && npm run export && gh-pages -d out"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "^5.0.0"
  }
}
